/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/cursor-settings-guide-jp",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
