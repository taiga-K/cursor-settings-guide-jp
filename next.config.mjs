/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/cursor-settings-docs",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
